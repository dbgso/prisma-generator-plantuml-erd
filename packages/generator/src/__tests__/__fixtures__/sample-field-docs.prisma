datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    Int    @id @default(autoincrement())
  /// User email address
  email String @unique
  /// User full name
  name  String?

  @@map("users")
}
